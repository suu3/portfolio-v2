/**
 * Experiment switches.
 *
 * ENTRY_GATE: the playable entrance (`components/EntryGate`) that runs before
 * the splash on the first load of a session. Flip it to `false` to go straight
 * back to splash-only — nothing else has to change.
 */
export const ENTRY_GATE = true;
