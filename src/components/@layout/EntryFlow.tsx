"use client";

import SplashScreen from "@/components/SplashScreen";

/**
 * What covers the site on arrival. The playable entry gate is gone — the short
 * splash is all that's left.
 */
const EntryFlow = () => <SplashScreen />;

export default EntryFlow;
